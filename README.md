# Automotive Angular

Standalone Angular 17 frontend for the City Car automotive marketplace.

## Commands

```powershell
npm install
npm start
npm run build
```

The development server runs on Angular's Vite-backed dev server. Open:

```text
http://localhost:4200
```

The app calls `/api` when a backend is available and falls back to local demo data when it is not.
